<div class="page-header">
  <h3>New Comment</h3>
</div>

<%= simple_form_for [ticket, comment] do |f| %>
  <%= f.input :text %>
  <%= f.association :state, selected: ticket.state_id %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>